// fake data simulating tickets the logged in test user owns
// keyed by raffle id, value is the array of ticket numbers they own
export const myTickets : Record<number, number[]> = {
  1: [12, 87, 203, 340],
  2: [5, 99 ],
}
